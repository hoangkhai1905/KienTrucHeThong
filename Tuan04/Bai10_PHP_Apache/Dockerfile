FROM php:8.2-apache

# Để tự copy có thể dùng: COPY index.php /var/www/html/
# Nhưng theo yêu cầu là mount từ máy host, ta chỉ cần tạo Dockerfile đơn giản:
EXPOSE 80
# Lệnh chạy: docker run -p 8080:80 -v ${PWD}:/var/www/html php-apache-app
